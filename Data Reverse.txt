def data_reverse(data)
    list=[]
    for i in range(0,int(len(data)8))
        list.append(data[int(0+i8)int(8+i8)])
    L=list[-1]
    final=[]
    for item in L
        for subitem in item
            final.append(subitem)
    return final